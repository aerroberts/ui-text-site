import type { CurrencyDefinition } from "../../control-flow/events/currency";

export const Currency: Record<string, CurrencyDefinition> = {
    heat: {
        name: "heat",
        symbol: "ঌ",
        color: "#FF0000",
        initialMaxValue: 100,
    },
};
